# NEXT

当前阶段：v0.4.0 主页小组件已实现、校验并打包；真实 Myriad 宿主仍未验收。

本阶段已取得证据：

- 新增 additive `moderator.resolve-report` 事件，只有 Room owner 的服务端 sender 能使举报进入已处理状态。
- 被封禁 actor 的作品继续保留在管理员审计数据中，但从公开画布排除。
- 协议回归测试增至 9 项并通过。
- 管理入口改为可关闭的全屏工作台，包含总览、内容审核、成员治理、画板归档和系统诊断五个模块。
- 后台可搜索署名、actor 与作品 ID，可定位、隐藏、恢复作品并标记举报已处理。
- 成员页读取真实 Room 成员，支持应用封禁/解除封禁和请求移出 Room，不提供角色修改。
- 归档页复用最近 6 块快照，系统页展示实际 Tapp 权限与事件接收/接受/忽略计数。
- `@myriad-you/tapp-cli@0.1.2` 校验返回 0 diagnostics；v0.3.0 安装包包含 10 个运行文件，大小 122224 bytes。

v0.4.0 新增：

- Manifest 声明 `board-preview` Widget，支持 `2x2` / `4x2`，默认 `4x2`。
- Widget 仅读取公开快照和活动画板配置，不加载编辑器、不读取 Room 历史。
- 新发布快照附带公开作品数与粗网格占用率；旧快照保持兼容。
- Widget 监听共享快照变更并请求可见实例刷新，支持亮色/暗色和中英日文案。
- Widget 快照渲染测试 1 项与原有协议回归测试 9 项全部通过。
- `@myriad-you/tapp-cli@0.1.2` 返回 0 diagnostics；v0.4.0 安装包包含 14 个运行文件，大小 139756 bytes。

已完成证据：

- 9 项协议测试通过：限制校验、曲线采样、横向/旧版画布、信封解码、管理员审核、举报处理、封禁审计过滤和确定性占用率。
- Manifest/文件/禁用直连网络自检通过。
- `main.js`、画布编辑器、运行时和入口文件均通过 Node 语法检查。
- `preview.html` 可用于低成本静态界面检查；它不证明 Tapp Bridge 行为。

完成代码后需要在真实 Myriad 宿主中验证：

- 普通成员没有 `federation:room`，但加入 Room 后可以读写自定义消息。
- `sender_actor` 在历史与实时消息中稳定，消息分页顺序正确。
- 管理员移除成员后，公开 Room 是否允许其自行重新加入。
- 自定义签名板消息是否会污染 Myriad 原生聊天界面。
- 256 KiB 与 30 秒限制在宿主/服务器侧是否还有更严格约束。

未经这些证据，不把运行时验收标记为通过。
