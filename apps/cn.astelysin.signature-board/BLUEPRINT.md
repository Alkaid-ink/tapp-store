# First Playable Blueprint

1. 以无依赖 Canvas 2D 编辑器完成绘制、缩放、平移、撤销和提交前擦除。
2. 以可离线测试的纯函数模块校验消息、重放历史、计算占用率并拒绝越界数据。
3. 访客读取 `Tapp.shared` 快照；成员读取/订阅专用 Room；管理员创建、审核、封禁、归档和发布快照。
4. 用静态预览确认桌面与移动布局；用最小 Node 测试确认协议边界。

首版之后才考虑性能分片、增量快照、更复杂的历史检索或服务端强制风控。
